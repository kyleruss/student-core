//####################################
//  KYLE RUSSELL
//  13831056
//  PDC Project
//####################################

package engine.config;

import java.util.Properties;
import static org.junit.Assert.assertNotNull;
import static org.junit.Assert.assertNull;
import org.junit.Test;


public class DatabaseConfigTest 
{
    
   /* @Test
    public void testFactories()
    {
        Properties ptest        =   new Properties();
        
        DatabaseConfig conf1    =   DatabaseConfig.config(); //test default factory
        DatabaseConfig conf2    =   DatabaseConfig.config(ptest); //test factory with passed config
        
        assertNotNull(conf1);
        assertNotNull(conf2);
    }
    
    @Test
    public void testGet() 
    {
       DatabaseConfig conf  =   new DatabaseConfig();
       String real          =   (String) conf.get("server"); //test existing entry
       String fake          =   (String) conf.get("bogus"); //test bogus entry
       assertNotNull(real);
       assertNull(fake);
    } */
    
}
