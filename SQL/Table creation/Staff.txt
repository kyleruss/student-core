CREATE TABLE staff
(
    id INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY(START WITH 1, INCREMENT BY 1),
    salary INTEGER,
    title varchar(40),
    manager_id varchar(16),
    FOREIGN KEY(manager_id) REFERENCES users(username) ON DELETE SET NULL
);